[project]
name = "pr-pilot"
version = "0.1.0"
description = "Claude Code skill: polls GitHub PRs and orchestrates review/fix loops"
requires-python = ">=3.11"

[dependency-groups]
dev = ["pytest", "ruff"]

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
